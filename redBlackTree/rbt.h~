#include <iostream>
using namespace std;

struct node {
  int data;
  bool color;
  node* parent;
  node* left;
  node* right;
};

class redBlackTree() {
 public:
  redBlackTree();
  insert();
 private:
}
