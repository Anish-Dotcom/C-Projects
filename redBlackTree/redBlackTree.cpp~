#include <iostream>
#include "redBlackTree"
using namespace std;

redBlackTree() {
  node* root = NULL;
}

void redBlackTree::insert(node* current, node* newnode,int data) {
  node* newnode = new node;
  newnode->data = data;
  if(current == NULL) {
    
  }
}
