#include <iostream>
#include "redBlackTree.h"
using namespace std;

redBlackTree() {
  node* root = NULL;
}

void redBlackTree::insert(int data) {
  node* newnode = new node;
  newnode->data = data;
  if(root == NULL) {
    root = newnode;
    return;
  }
  insertTraversal(root, newnode);
}

void redBlackTree::insertTraversal(node* current, node* newnode) {
  
}
